#include "main.h"
#include <stdio.h>

/**
 *print_alphabet - check the code
 *
 * Return: no return
 */

void print_alphabet(void)
{
int letter;

for (letter = 'a'; letter < = 'z'; letter++)

	_putchar (letter + "\n");
}
_putchar (10);
}
